// Importation du composant Homepage depuis le dossier (homepage)
import Homepage from "./(homepage)/page";

/**
 * Composant racine de l'application
 * Sert de point d'entrée principal et enveloppe le composant Homepage
 * @returns Le composant Homepage qui contient le contenu principal de la page
 */
export default function Home() {
  return (
    <Homepage>
      
    </Homepage>
  );
}
